# Claude Code local state (session notes, prompts, settings, generated skill).
/.claude/

# Go build output: the CLI binary, left in the tree by a bare `go build`.
/ot4xb-tool
/ot4xb-tool.exe
/cmd/ot4xb-tool/ot4xb-tool
/cmd/ot4xb-tool/ot4xb-tool.exe

# Go test artefacts: `go test -c` binaries and coverage profiles.
*.test
*.test.exe
coverage.out
coverage.html

# Editor and OS leftovers.
*.bak
*.orig
*~
Thumbs.db
desktop.ini
__publish.bat
